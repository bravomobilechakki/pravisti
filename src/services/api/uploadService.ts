import {
  uploadService,
  uploadImage,
  uploadMultipleImages,
  resolveImageUrl,
  deleteImage,
  validateImage,
  MAX_IMAGE_SIZE_BYTES,
  ALLOWED_IMAGE_TYPES,
  ALLOWED_IMAGE_EXTENSIONS,
} from '../uploadService';

export {
  uploadService,
  uploadImage,
  uploadMultipleImages,
  resolveImageUrl,
  deleteImage,
  validateImage,
  MAX_IMAGE_SIZE_BYTES,
  ALLOWED_IMAGE_TYPES,
  ALLOWED_IMAGE_EXTENSIONS,
};

export default uploadService;
